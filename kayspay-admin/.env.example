# Copy to .env.local (already gitignored via the repo's .env*.local rule).
# Both values are the same public anon-key pair the mobile app ships with —
# safe client-side, never the service-role key.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
